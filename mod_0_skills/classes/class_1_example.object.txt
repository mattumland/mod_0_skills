Object: StandardTable

Attributes:

numberOfSeats (4)
isFull (false)
height (40)
section ("main dining room")

Methods:

standardTable.seatFolks (change isFull to true)
standardTable.addChair (change the value of numberOfSeats to 5)
standardTable.relocate (change the value of section to "private room")
standardTable.removeChair (change the value of numberOfSeats back to 4)
